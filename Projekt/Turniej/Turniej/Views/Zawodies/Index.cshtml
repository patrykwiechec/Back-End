@model IEnumerable<Turniej.Models.Zawody>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nazwa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Lokalizacja)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nazwa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lokalizacja)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdZawodow">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdZawodow">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdZawodow">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
